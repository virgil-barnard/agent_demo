---
description: Execute exactly one ready GitHub Issue with evidence
agent: build
---

Execute exactly one ready GitHub Issue according to your build prompt and `AGENTS.md`.

Optional issue number or additional constraint:

$ARGUMENTS

Do not execute more than one issue in this invocation.

